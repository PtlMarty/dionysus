<%= simple_form_for service do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price_hours %>

  <%# <%= f.input :image we need this in the future %>
  <%= f.submit 'Create' %>
  <%# <%= f.button :submit %>
<% end %>
