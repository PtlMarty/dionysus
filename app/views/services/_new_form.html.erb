<%= simple_form for services do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price_hours %>
  <%# <%= f.input :image we need this in the future %>
  <%= f.button :submit %>
<% end %>
